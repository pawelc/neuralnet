

requre "torch"
local weights = torch.rand Tensor(10,10,2)
local input = torch.Tensor({2,2})

output=input-weights

layer.deltaWeights[x][y]=(layer.input-layer.weights[x][y])*tn[x][y]*learningRate  

  
 